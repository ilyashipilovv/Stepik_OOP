from itertools import combinations


def inversions(it):
    combs = list(combinations(it, 2))
    return sum(map(lambda x: x[0] > x[1], combs))




sequence = [4, 3, 2, 1]

print(inversions(sequence))